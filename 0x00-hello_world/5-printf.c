#include <stdio.h>

/**
 * main - the main function to use printf
 *
 * Return: 0 if program success
 */
int main(void)
{

	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
